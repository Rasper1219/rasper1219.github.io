---
import Base from "../../layouts/BaseLayout.astro";
import { SITE } from "../../data/siteConfig";

const IMAGES = [
  { src: SITE.base + "images/about/me-1.jpg", alt: "Me at the Wisconsin - Alabama game" },
  { src: SITE.base + "images/about/me-2.jpg", alt: "Playing Club Baseball in South Carolina" },
  { src: SITE.base + "images/about/me-3.jpg", alt: "My Family and I at my Undergrad graduation" },
];
---
<Base title="About">
  <h1 class="text-3xl font-bold">About</h1>

  <div class="card mt-4">
    <p>Write 3–5 sentences about what you like to solve, how you work, and where you’re headed.</p>
    <ul class="list-disc pl-6 mt-3 text-[var(--muted)]">
      <li>Conscientious • Constructive • Passionate</li>
      <li>Python &amp; ML focus</li>
      <li>Dark, neo-retro Swiss design</li>
    </ul>
  </div>

  <section class="mt-6">
    <h2 class="text-xl font-semibold">Photos</h2>
    <div class="mt-4 grid grid-cols-1 sm:grid-cols-3 gap-4">
      {IMAGES.map((img) => (
        <figure class="card p-0 overflow-hidden">
          <img
            src={img.src}
            alt={img.alt}
            loading="lazy"
            decoding="async"
            class="w-full h-64 object-cover"
          />
        </figure>
      ))}
    </div>
  </section>
</Base>
